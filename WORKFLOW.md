# WORKFLOW — 멀티에이전트 작업 프로토콜

## 문서 계층 구조

| 파일 | 역할 | 업데이트 시점 |
|------|------|-------------|
| `CLAUDE.md` | 작업 규칙, 워크플로우 요약, 완료 이력 | 워크플로우 변경 or 마일스톤 완료 시 |
| `DEV_GUIDE.md` | 전체 아키텍처 지도, 데이터 흐름, 금지사항 색인 | 구조/아키텍처 변경 시 |
| `WORKFLOW.md` | 멀티에이전트 프로토콜 상세 (이 파일) | 작업 방식 자체가 바뀔 때 |
| `TASKS.md` | 현재 스프린트 태스크 목록 (최대 3개) | 매 작업 사이클마다 |
| `BACKLOG.md` | 발견된 이슈 + 다음 스프린트 대기 태스크 | 이슈 발견 즉시 |
| `각폴더/_GUIDE.md` | 폴더별 패턴, 금지사항, 최근 변경 | 해당 폴더 코드 변경 시 |

> **CLAUDE.md와 DEV_GUIDE.md는 "작업 규칙서"와 "지도"다. 일지가 아니다.**
> 로그/감사/테스트 결과는 `docs/` 폴더에 보관.

---

## 스프린트 태스크 관리 규칙

### 최대 3개 규칙
- TASKS.md에는 **진행 중인 태스크 최대 3개**만 올린다
- 나머지는 BACKLOG.md에서 대기
- 3개 완료 → docs/ 아카이브 → BACKLOG.md에서 다음 3개 올림

### 태스크 발굴 채널
| 출처 | 처리 방법 |
|------|---------|
| 작업 중 발견 (A 태스크 관련) | 해당 태스크 "테스트 피드백" 섹션에 기록 |
| 작업 중 발견 (A 태스크 무관) | BACKLOG.md에 한 줄 즉시 추가 |
| 감사 보고서 / 스프린트 회고 | BACKLOG.md에 추가 후 다음 스프린트 기획 시 반영 |

### 태스크 연장 vs 백로그 기준
**현재 태스크 연장으로 처리**:
- 현재 태스크 테스트 통과에 직접 영향을 주는 문제
- 수정 범위가 2~3줄 이내
- 고치지 않으면 완료 선언 불가

**BACKLOG.md로 넘김**:
- 현재 태스크 테스트는 통과하지만 별도 개선이 필요한 것
- 다른 파일/모듈에 영향
- 설계 고민이 필요한 것

---

## 표준 멀티에이전트 워크플로우 (Agent Teams)

복잡한 구현 작업은 Claude Code Agent Teams로 진행한다.

```
[팀 리드 (메인 세션)]
  └─ TASKS.md 검토 + 팀 구성 지시
  └─ 태스크별 팀원 배정
       ↓
[구현 팀원 × N]  ← 독립 태스크 병렬 실행
  └─ 태스크 구현
  └─ harness 실행 (자기 폴더)
  └─ TASKS.md 상태 업데이트
       ↓
[테스트 팀원]
  └─ python scripts/harness.py all 실행
  └─ 실패 시 해당 태스크 "테스트 피드백" 섹션에 심각도 포함 기록
  └─ 구현 팀원에 재작업 요청 (max 10회)
       ↓
[최종 확인 팀원]
  └─ 완료 체크리스트 확인
  └─ docs/ 아카이브 + BACKLOG.md 정리
  └─ CLAUDE.md 완료 이력 업데이트
```

### 팀원 역할 요약

| 팀원 | 도구 | 산출물 |
|------|------|--------|
| 팀 리드 | Read, Glob, Grep, Edit | TASKS.md 설계 구상 작성 + 팀 구성 + 전체 조율 |
| 구현 팀원 | Read, Edit, Write, Bash | 코드, 테스트 파일 |
| 테스트 팀원 | Bash(harness), Read | TASKS.md 피드백 섹션 |
| 최종 확인 팀원 | Bash(harness all), Edit | 체크리스트, 문서 업데이트 |

> 팀 리드가 플래닝(TASKS.md 설계 구상 작성)까지 겸한다.
> 구현 팀원은 TASKS.md를 읽고 바로 구현 시작.

---

## TASKS.md 태스크 스키마

```markdown
## B-NNN: 태스크 이름
**상태**: pending | in_progress | completed | blocked
**우선순위**: high | medium | low
**관련 파일**: 수정/생성할 파일 목록

### 설계 구상
왜 이 방식을 선택했는지.
**✅ 선택한 이유**: ...
**❌ 대안 제외 이유**: ...

### 구현 세부사항
- 함수 시그니처
- 데이터 흐름
- 핵심 로직 포인트

### 완료 기준
- 구체적인 테스트 조건 (단순 "테스트 통과"보다 구체적으로)

### 제약사항
- 건드리면 안 되는 것
- Silent Failure 가능 지점

### 테스트 피드백
(테스트 팀원 기록 — A 태스크 관련 이슈만)
심각도: 🔴 Critical | 🟠 High | 🟡 Medium | 🟢 Low

### 최종 확인
- [ ] harness all 통과
- [ ] _GUIDE.md 업데이트
- [ ] BACKLOG.md 이슈 정리
- [ ] 실제 구현 범위 기록 (단순화된 것 있으면 명시)
```

---

## 이슈 심각도 기준

| 심각도 | 의미 | 예시 |
|--------|------|------|
| 🔴 Critical | 결과 자체가 무효화됨 | lookahead bias, 순환 논리 |
| 🟠 High | 수치 크게 왜곡됨 | 수식 오류, 잘못된 proxy |
| 🟡 Medium | 구조적으로 잘못됐지만 결과 왜곡 제한적 | 고정값 하드코딩 |
| 🟢 Low | 개선 여지 있으나 치명적이지 않음 | 주석 미기재 |

---

## Silent Failure 체크 원칙

```
□ except Exception: pass/continue 패턴이 있는가?
□ 빈 dict {} 반환 시 호출자가 알아챌 수 있는가?
□ 기본값(None, 0, False)으로 fallback할 때 로그가 있는가?
□ 테스트가 Mock으로만 커버되어 실제 실패를 잡지 못하는 지점이 있는가?
```

---

## 언제 Agent Teams를 쓰는가

**쓸 것**: 새 모듈 생성, 멀티 파일 수정, 파이프라인 통합, 3개 태스크 동시 진행

**쓰지 않을 것**: 단순 버그 수정 1개, 프롬프트 텍스트 수정, _GUIDE.md 업데이트만

---

## 병렬 실행 규칙

의존성이 없는 태스크는 구현 팀원을 병렬로 배정한다.

```
예) B-001 (독립) ← 팀원 A
    B-002 (독립) ← 팀원 B  → 병렬 실행
    B-003 (독립) ← 팀원 C
```

의존 관계는 TASKS.md에 `**의존**: B-NNN` 형태로 명시.

---

## 피드백 루프 규칙

- 테스트 실패 시 TASKS.md 피드백 섹션에 심각도 포함 기록 후 구현 팀원 재실행
- **동일 에러 2회 연속** → 설계 구상 재검토
- 10회 초과 시 사용자에게 보고 후 대기
- 피드백에서 발견한 패턴 → 해당 폴더 `_GUIDE.md` 금지사항에 추가
- 🔴 Critical 발견 시 → blocked, 사용자에게 즉시 보고

---

## 스프린트 완료 시 정리

```
1. 완료된 태스크 전체 → docs/SPRINT_YYYY-MM-DD_이름.md 아카이브
2. BACKLOG.md 검토
   - 대기 중인 태스크 목록 확인 → 다음 스프린트 3개 선정
   - 이슈 테이블 확인 → 태스크급으로 커진 것 있으면 대기 중인 태스크로 승격
   - 완료된 항목 삭제
3. TASKS.md → 선정된 3개로 새 스프린트 작성
4. CLAUDE.md 현재 스프린트 섹션 업데이트
```

> BACKLOG.md가 비어있으면 다음 스프린트 없음 — 사용자에게 확인 후 대기.

---

## 감사 보고서 갱신

`docs/SYSTEM_AUDIT_REPORT_v2.md` 갱신은 **사용자가 명시적으로 요청할 때만** 수행.

스프린트 완료 시 자동 실행하지 않는다. 주요 시스템 완성 시점에 사용자 판단으로 트리거.

---

## 병렬 에이전트 TASKS.md 편집 충돌 방지

- 각 팀원은 **자기 태스크 섹션만** 편집
- 스프린트 상태 변경(아카이브 등)은 최종 확인 팀원만 처리

---

*마지막 갱신: 2026-04-21 — Agent Teams 반영, BACKLOG.md 추가, 최대 3개 규칙, 태스크 연장 기준*
